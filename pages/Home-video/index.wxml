<!-- video 页面 -->
<view class="video">
    <view class="item" wx:for="{{videoInfoList}}" wx:key="id">
        <video-item item="{{item}}" bindtap="clickVideoInfo" data-videoItemDetail="{{item}}"></video-item>
    </view>
</view>
 